# Tasks: CAPSTONE-UI — React 19 SPA + System-wide ES/EN i18n + Backend Delta

Change: `capstone-ui` · Project: `temporada-1` · Phase: **tasks** · Artifact: hybrid
Date: 2026-09-22 · Upstream: proposal.md + 6 delta specs + design.md (8 ADRs) · Downstream: apply
STRICT TDD: RED→GREEN→TRIANGULATE→REFACTOR. Backend deltas: `node --test` + supertest vs real Postgres. Frontend: vitest + RTL + jsdom (mock fetch is the single seam; tests NEVER hit live backend). Every test name prefixed `R-XXX-N:`. Evidence: `docs/output-ui-it<N>.txt` per iteration.

**Sequencing resolutions (binding for apply)**: S1 it1 backend delta FIRST (UI builds against the real contract — T-1-5 gates T-3-6, T-1-6 gates T-3-7, T-1-7 gates T-5-5). S2 ui bootstrap cluster (T-2-1..T-2-3) gates ALL it2+ tasks; the vitest+RTL spike (T-2-2) must conclude before any RTL test task. S3 i18n key catalog PINNED at T-2-4; later iterations add keys ONLY via review (cumulative R-I18N gate). S4 api client (T-2-5) precedes every module page (it3+); RBAC mirror (T-2-7) precedes RoleGate wiring. S5 it6 polish/evidence last; conditional tasks (stock inactive exclusion, WAREHOUSE_IN_USE verification) resolve additively at it6. v1 vanilla `ui/index.html`+`app.js` removed at T-2-1 (R-PROD-7 modified; git history retains for revert).

---

## Iteration 1 — Backend i18n delta (it1)

**Goal**: additive delta green end-to-end: bilingual notifications (per-recipient locale, immutable snapshot), self-service locale, products PATCH, warehouses CRUD (WAREHOUSE_IN_USE), jobs filters, SPA fallback serving. **Exit**: `tsc --noEmit` backend clean, full `node --test` suite green (229 + new, zero regressions R-PROD-3), `docs/output-ui-it1.txt`.

- [x] **T-1-1** Migration `007_locale.sql`: `users.locale` + `notifications.locale` (`TEXT NOT NULL DEFAULT 'es' CHECK IN ('es','en')`, `ADD COLUMN IF NOT EXISTS`). Contract R-BE-1. RED: `R-BE-1: applies twice → no-op, defaults 'es', CHECK rejects 'fr'`. Files: `backend/src/db/sql/007_locale.sql`, migration tests. Deps: none (runner exists). (S)
- [x] **T-1-2** `jobs/templates.ts` bilingual: `{es,en}` dicts for all 7 types, `render(type, payload, locale='es')`; `user_invited` never renders passwords. Contracts R-BE-2, R-NOT-5. RED: `R-BE-2: key parity es≡en all types` + `R-BE-2: neutral Spanish scan zero banned tokens` + `R-NOT-5: no password in either locale`. Files: `backend/src/jobs/templates.ts`, `tests/unit/templates.test.ts` (extend). (M)
- [x] **T-1-3** `modules/notifications/emit.ts` per-recipient locale: recipient query selects `locale`, render per recipient, INSERT stores `title, body, locale`; email job still carries only `notificationId` (worker never re-renders). Contract R-BE-3. RED: `R-BE-3: es+en recipients render separately, locale stored` + `R-BE-3: replay → exactly-once, snapshot unchanged`. Files: `backend/src/modules/notifications/emit.ts`, `repository.ts`. Deps: T-1-1, T-1-2. (M)
- [x] **T-1-4** `PATCH /api/auth/me {locale}` (zod `.strict()`, 422 invalid / 401 unauth) + `PublicUser.locale` surfaced in login/refresh/me/users DTOs; `POST /api/users` does NOT accept locale. Contract R-BE-4. RED: `R-BE-4: set locale 200 + row updated` / `R-BE-4: invalid 'fr' → 422 unchanged` / `R-BE-4: dto surface all user objects`. Files: `backend/src/modules/auth/{dto,service,repository,controller,routes}.ts`. Deps: T-1-1. (M)
- [x] **T-1-5** `PATCH /api/products/:id` `{name?, sku?, lowStockThreshold?, active?}` (≥1 field refine; perm `stock:product_manage`; 404 unknown; 409 `DUPLICATE_SKU`). Contract R-UI-CRM-7 (delta approved). RED: `R-UI-CRM-7: deactivate → inactive product absent from GET /api/stock` + `R-UI-CRM-7: dup sku → 409`. Files: `backend/src/modules/stock/*`. (M)
- [x] **T-1-6** Warehouses: `GET /api/warehouses` (`stock:stock_read` — operators need pickers) `{data,pagination}`; `PATCH /:id {name}` + `DELETE /:id` (`stock:product_manage`); DELETE 204 only when no movement references the warehouse, else 409 additive subcode `WAREHOUSE_IN_USE` (documented). Contract R-UI-CRM-8. RED: `R-UI-CRM-8: create/list/patch/delete 204` + `R-UI-CRM-8: delete in-use → 409 WAREHOUSE_IN_USE`. Files: `backend/src/modules/stock/*`. Deps: T-1-5 (same files). (M)
- [x] **T-1-7** Jobs server-side filters: `jobsListQuerySchema` + `state` (pg-boss states — NOTE: 6 in installed v12 enum, see apply-progress) + `queue` (≤100) optional; repository parameterized WHERE + COUNT parity. Contract R-UI-JOB-1 (resolved server-side). RED: `R-UI-JOB-1: state=completed filters rows, queue filter, COUNT matches`. Files: `backend/src/modules/jobs/{dto,repository,service,controller}.ts`. (S)
- [x] **T-1-8** SPA serving: `middleware/spaFallback.ts` (plain middleware: GET + non-`/api` → `sendFile(index.html)`; NOT `app.get('*')` — Express 5 path-to-regexp pitfall) + `app.ts` ordering: API routers → static `uiDistDir` (injectable dep, default `../../ui/dist`) → fallback → JSON 404 → errorHandler. Contract R-BE-5. RED: `R-BE-5: deep link /orders/42 → 200 index.html` / `R-BE-5: /api/does-not-exist → JSON 404` / `R-BE-5: static asset wins` (temp fixture dir — hermetic, no Vite build). Files: `backend/src/middleware/spaFallback.ts`, `backend/src/app.ts`. (M)
- [x] **T-1-9** OpenAPI regen from zod (R-DOC-1: docs list `PATCH /api/auth/me` + locale field) + backend integration tests (`R-BE-1..5`, `R-NOT-5`, `R-UI-CRM-7/8`, `R-UI-JOB-1` scenarios) + full-suite rerun + `docs/output-ui-it1.txt` (green tests per requirement + demo commands). (M)

## Iteration 2 — UI foundation (it2; bootstrap cluster T-2-1..T-2-3) — ✅ COMPLETE (Batch B1 it2a + Batch B2 it2b, 84 vitest, 256 backend)

**Goal**: `ui/` workspace on strict stack with first-ever vitest+RTL runner (spike done), i18n infra + parity + banned-token gate, single-flight api client, auth session (boot restore, bounded refresh, logout), RBAC guards + shell (sidebar, toggle, dark theme). **Exit**: `tsc --noEmit -w ui` clean, vitest green (R-AUTHUI, R-UI-FND, R-RBAC, R-I18N, R-UI-NFR infra), `docs/output-ui-it2.txt` — ✅ met (84 vitest, tsc clean, backend 256/256).

- [x] **T-2-1** ui/ scaffold: `package.json` (react 19, react-dom 19, react-router-dom 7, vite 6, @vitejs/plugin-react, babel-plugin-react-compiler, vitest, @testing-library/react, user-event, jest-dom, jsdom), strict `tsconfig.json` (noUncheckedIndexedAccess, exactOptionalPropertyTypes, erasableSyntaxOnly), `vite.config.ts` (compiler plugin + `/api` proxy → :3000 + vitest config), root scripts `typecheck`/`test`/`build` → both workspaces; DELETE v1 `ui/index.html`+`app.js` (R-PROD-7 modified). RED: `tsc --noEmit` probe fails → green; `vitest run` smoke. Files: `capstone/ui/*`, `capstone/package.json`. Deps: it1 merged. (L)
- [x] **T-2-2** ⚠️ **SPIKE (design flag d — do NOT skip)**: first vitest+RTL component test (jsdom env, React 19 + Compiler interplay) — proves RTL/user-event/act work before any page test. RED: spike test fails without jsdom env → green. Files: `docs/spike-vitest-rtl.md`, scratch test (removed after). Deps: T-2-1. (M)
- [x] **T-2-3** Mock fetch harness: `tests/helpers/mockFetch.ts` (`vi.stubGlobal('fetch')`, records `{method,path,headers,body}`, scripted responses) + `tests/__fixtures__/api.ts` (bodies mirroring zod/OpenAPI shapes). RED: `R-UI-NFR-6: contract test asserts recorded request shape`. Files: `ui/tests/helpers/*`, `ui/tests/__fixtures__/*`. Deps: T-2-2. (M)
- [x] **T-2-4** i18n infra: `i18n/types.ts`, `locales/{es,en}.ts` (MessageKey = keyof typeof en), `LocaleContext`, `useTranslation`, `localizeError` (incl. CONFLICT subcodes + `WAREHOUSE_IN_USE` + unknown fallback — R-I18N-4), `formatters.ts` (float-free D13 — R-UI-ORD-6). Key catalog PINNED here (S3 gate). RED: `R-I18N-1: key parity es≡en` + `R-I18N-2: banned tokens zero (UI dicts)` + `R-I18N-4: unknown code → generic fallback` + `R-UI-ORD-6: "12.50" → "12,50 €" / "$12.50" / round-trip`. Files: `ui/src/i18n/*`, `ui/tests/i18n.test.ts`, `formatters.test.ts`. Deps: T-2-3. (L)
- [x] **T-2-5** api client: `api/client.ts` (memory-only token, single-flight refresh, bounded 401→refresh→retry once→onSessionExpired; login/refresh never trigger refresh path; `ApiClientError {status,code,message}`), `api/types.ts` (Paginated, D13, entity interfaces), `apiFetchRaw` for 201-vs-200 replay. RED: `R-AUTHUI-1: 4 concurrent 401s → exactly 1 refresh, all retried` / `R-AUTHUI-1: refresh fails → ≤1 attempt, state cleared` / `R-UI-FND-4: replay 200 ≠ 201`. Files: `ui/src/api/client.ts`, `types.ts`, `ui/tests/api-client.test.ts`. Deps: T-2-3. (L)
- [x] **T-2-6** Auth + guards: `auth/AuthContext.tsx` (boot restore via refresh — single round trip, R-AUTHUI-4; logout = raw-fetch `POST /api/auth/logout` THEN clear memory, R-AUTHUI-3), `components/ProtectedRoute.tsx` (`?next=` preservation + loading state), `PermissionRoute.tsx` (redirect `/dashboard` + denial toast, no API call — R-RBAC-1), `RoleGate.tsx` (hide, never disable — R-RBAC-2). RED: `R-AUTHUI-4: boot on /orders restores silently, no login flash` + `R-AUTHUI-3: logout fires API then lands /login`. Files: `ui/src/auth/*`, `ui/src/components/{ProtectedRoute,PermissionRoute,RoleGate}.tsx`, `ui/tests/auth.test.tsx`. Deps: T-2-5. (M)
- [x] **T-2-7** `auth/permissions.ts`: const `ROLE_PERMISSIONS` mirror (5 roles × 19 codes, derived `Role`/`RolePermission` const-types). Parity test imports backend registry directly across workspaces (design flag c — RESOLVED: direct import works for vitest AND tsc; no JSON fixture needed). RED: `R-RBAC-3: matrix parity deep-equal backend ROLE_PERMISSIONS`. Files: `ui/src/auth/permissions.ts`, `ui/tests/rbac.test.tsx`. Deps: T-2-4. (S)
- [x] **T-2-8** Login + shell: `pages/LoginPage.tsx` (localized identical-401, pre-auth toggle, `useActionState`), `components/Layout.tsx` + `Sidebar.tsx` (role-aware nav per R-RBAC-3 matrix, active highlight, user chip, unread-badge slot), `LocaleToggle.tsx` (pre-auth localStorage only / post-auth `PATCH /api/auth/me` + backend-wins-on-boot via `LocaleBackendSync` — R-I18N-3, R-BE-4), `ToastProvider.tsx`, `App.tsx` routes (lazy governance pages — R-UI-NFR-5: separate AuditPage/UsersPage/JobsPage chunks), `styles/index.css` (dark `#0f1115` / indigo `#4f46e5`). RED: `R-UI-FND-1: wrong password ES → one neutral message` + `R-I18N-3: pre-auth toggle persists, no API call` + `R-UI-FND-2: viewer nav = 6 modules, zero write actions`. Files: `ui/src/{pages,components,styles}/*`, `ui/tests/auth.test.tsx` (login). Deps: T-2-4..T-2-7. (L)
- [x] **T-2-9** Shared components + hooks: `DataTable`, `Pagination`, `Modal` (focus trap/ESC), `FormField` (+ Input/Select), `Badge`, `EmptyState`, `ErrorState`, `LoadingState`, `ConfirmButton` (two-step gate), `MoneyText`, `DebouncedSearchInput`; `hooks/{useResource,usePagination,useDebouncedValue}.ts`. RED: `R-UI-FND-3: empty list → localized empty state` + `R-UI-FND-3: 500 → error state, retry re-fetches`. Files: `ui/src/components/*`, `ui/src/hooks/*`, `ui/tests/foundation.test.tsx`. Deps: T-2-4, T-2-5. (M)
- [x] **T-2-10** Foundation tests + evidence: `R-UI-FND-4: double-click same key → one row` / `R-AUTHUI-2: storage scan — no token keys` / `R-RBAC-1: viewer /users → redirect, no request`; `docs/output-ui-it2.txt` (84 vitest, tsc clean, build chunks, backend 256/256). (M)

## Iteration 3 — Dashboard + CRM (it3) — ✅ COMPLETE (Batch C, 42 vitest)

**Goal**: practical landing page (status/KPI/low-stock/notifications) + customers CRUD + products CRUD + warehouses (Stock module tab) — all localized, role-gated, 409/422 mapped. **Exit**: vitest green (R-UI-DSH-*, R-UI-CRM-*), tsc clean, `docs/output-ui-it3.txt`.

- [x] **T-3-1** `pages/DashboardPage.tsx`: status card (`/api/status` + `/api/health`), KPI cards (customers/products/orders via `limit=1` pagination.total), low-stock card (client-side `level <= threshold` from `/api/stock` + `/api/products` — R-UI-DSH-2), recent-unread card (5), refresh button (stale-while-revalidating — R-UI-DSH-4). RED: `R-UI-DSH-1: partial failure — status degraded, counts render` + `R-UI-DSH-2: alerts present / none-low states`. Files: `ui/src/pages/DashboardPage.tsx`, `modules/dashboard/*`, `ui/tests/dashboard.test.tsx`. Deps: T-2-8 (api/auth modules). (M)
- [x] **T-3-2** `api/customers.ts` + `pages/CustomersPage.tsx`: list/search (debounced q)/status filter/pagination (R-UI-CRM-1); 403 → localized denial toast. RED: `R-UI-CRM-1: search "ana" → only Ana renders`. (M)
- [x] **T-3-3** Customer create/edit forms (Modal): client validation (name required, email format) + 422 field-level + 409 `DUPLICATE_EMAIL` localized, form intact. Contracts R-UI-CRM-2/3. RED: `R-UI-CRM-2: dup email → localized 409, data intact` + `R-UI-CRM-3: edit phone → PATCH {phone} only`. Files: `ui/src/modules/crm/*`, `ui/tests/customers.test.tsx`. Deps: T-3-2. (M)
- [x] **T-3-4** Deactivate/reactivate (ConfirmButton gate; cancel fires nothing; inactive hidden from default list, reactivate from inactive filter). Contract R-UI-CRM-4. RED: `R-UI-CRM-4: confirm gate — cancel → zero requests`. Deps: T-3-2, T-2-9. (S)
- [x] **T-3-5** `api/products.ts` + `pages/ProductsPage.tsx`: list + search + inactive badge (R-UI-CRM-5); create form (sku regex, threshold int ≥ 0, 409 `DUPLICATE_SKU` localized — R-UI-CRM-6). RED: `R-UI-CRM-6: threshold −1 → client block, no request` + `R-UI-CRM-5: inactive row shows badge`. (M)
- [x] **T-3-6** Product edit/deactivate via `PATCH /api/products/:id` (requires T-1-5): confirm-gated deactivate → inactive badge + absent from Stock levels; dup SKU 409 on edit. Contract R-UI-CRM-7. RED: `R-UI-CRM-7: deactivate → row inactive, absent from levels`. Deps: T-3-5, T-1-5. (M)
- [x] **T-3-7** Warehouses in Stock module: list from `GET /api/warehouses` (R-UI-CRM-8 resolved — no derived-from-stock workaround) + create form (`POST`, 422 localized); pickers reuse the list (operator sees for adjust — scenario). Requires T-1-6. RED: `R-UI-CRM-8: create 201 → appears; empty name → 422 localized`. Files: `ui/src/modules/stock/warehouses*`, `ui/tests/warehouses.test.tsx`. Deps: T-1-6. (M)
- [x] **T-3-8** CRM page tests + evidence: customers/products/warehouses page tests (mock fixtures), requirement-referenced names; `docs/output-ui-it3.txt`. (M)

## Iteration 4 — Stock + Orders (it4, showpiece) — ✅ COMPLETE (Batch C, 29 vitest)

**Goal**: stock levels/movements/adjust/transfer with zero-side-effect 409 UX + orders create/confirm (optimistic, atomic)/cancel; D13 money everywhere; idempotency keys per mutation. **Exit**: vitest green (R-UI-STK-*, R-UI-ORD-*), tsc clean, `docs/output-ui-it4.txt`.

- [x] **T-4-1** `api/stock.ts` (types `{items: StockRow[]}` — envelope deviation documented) + Stock page tabs: levels table (string levels, `level <= threshold` badge, productId/warehouseId filters). Contract R-UI-STK-1. RED: `R-UI-STK-1: level "3" threshold 5 → low badge; equal counts low` + `R-UI-STK-1: warehouse filter`. (M)
- [x] **T-4-2** Movements tab: `GET /api/stock/movements` newest-first, filters (product/type), pagination; refresh after mutations. Contract R-UI-STK-2. RED: `R-UI-STK-2: type=adjustment filter → only adjustment rows`. (S)
- [x] **T-4-3** Adjust form: signed non-zero qty, reason ≥10, fresh `Idempotency-Key` per submit; 409 `NEGATIVE_STOCK` localized + level cell unchanged; same-key replay 200 → exactly one movement. Contract R-UI-STK-3. RED: `R-UI-STK-3: qty −5 on level "3" → localized 409, cell stays "3"` + `R-UI-STK-3: replay 200 → one history row`. Files: `ui/src/modules/stock/*`, `ui/tests/stock.test.tsx`. Deps: T-4-1, T-2-5. (M)
- [x] **T-4-4** Transfer form: from≠to client block, qty ≥1, reason ≥10, idempotent; 409 `INSUFFICIENT_STOCK` → both cells unchanged. Contract R-UI-STK-4. RED: `R-UI-STK-4: same warehouse → localized block, no request` + `R-UI-STK-4: insufficient → cells unchanged`. (M)
- [x] **T-4-5** `api/orders.ts` + `pages/OrdersPage.tsx` (state filter, localized badges "Borrador/Confirmada/Cancelada", pagination, actions per state — R-UI-ORD-1) + `OrderDetailPage.tsx` (lines + D13 totals, localized 404 — R-UI-ORD-2). RED: `R-UI-ORD-1: status=confirmed → 2 rows` + `R-UI-ORD-2: unknown id → localized 404 state`. (M)
- [x] **T-4-6** Create order form: customer select + dynamic line editor (product picker, qty ≥1, unitPrice via `parseMoney` scale ≤2, add/remove line, ≥1 line client-enforced), `Idempotency-Key`; 422 `UNKNOWN_PRODUCT`/409 localized, form intact; replay 200 → one order. Contract R-UI-ORD-3. RED: `R-UI-ORD-3: scale>2 → line error, no request` + `R-UI-ORD-3: zero lines → "al menos una línea"`. Files: `ui/src/modules/orders/*`. Deps: T-4-5, T-2-4 (formatters). (L)
- [x] **T-4-7** Confirm order: `useOptimistic` pending row + `ConfirmButton` two-step + `Idempotency-Key`; success → confirmed + toast + refetch stock/movements/badge; 409 `INSUFFICIENT_STOCK` → rollback, order stays draft, zero order_out rows shown; 409 `INVALID_STATE` → refresh row from server. Contract R-UI-ORD-4 (sequence diagram §4.2). RED: `R-UI-ORD-4: insufficient → draft + localized, no order_out rows` + `R-UI-ORD-4: confirm gate cancel → no request`. Deps: T-4-6, T-2-9. (L)
- [x] **T-4-8** Cancel order: reason modal ≥10 client-blocked; success → cancelled + toast, stock unchanged; 409 `INVALID_STATE` → server refresh. Contract R-UI-ORD-5. RED: `R-UI-ORD-5: short reason → client block` + `R-UI-ORD-5: cancel → cancelled, stock levels untouched`. (S)
- [x] **T-4-9** Stock+orders page tests + evidence: stock.test.tsx, orders.test.tsx (happy/insufficient/already-confirmed paths per fixtures); `docs/output-ui-it4.txt`. (M)

## Iteration 5 — Governance (it5) — ✅ COMPLETE (Batch D, 195 vitest, 256 backend, apply-progress #1434)

**Goal**: notifications (list/filter/mark-read/nav badge, stored snapshots), audit (filterable, JSON-text payload), users (admin list/create/invite/edit/activate), jobs (list with server-side filters + retry). **Exit**: vitest green (R-UI-NOT-*, R-UI-AUD-*, R-UI-USR-*, R-UI-JOB-*), tsc clean, `docs/output-ui-it5.txt`.

- [x] **T-5-1** `api/notifications.ts` + `pages/NotificationsPage.tsx`: unread-only filter, stored snapshot display (never re-rendered client-side — R-UI-NOT-1), mark-read idempotent 204 (R-UI-NOT-2), sidebar unread badge via `limit=1` pagination.total, refresh on mark-read + nav (R-UI-NOT-3). RED: `R-UI-NOT-1: en snapshot renders EN while UI locale es` + `R-UI-NOT-3: badge 5 → 3 after marks; zero → hidden`. Files: `ui/src/modules/governance/*`, `ui/tests/notifications.test.tsx`. Deps: T-2-8 (badge slot). (M)
- [x] **T-5-2** `api/audit.ts` + `pages/AuditPage.tsx`: filterable (entity/action/from/to), newest-first, payload as JSON text (never executed — R-UI-NFR-2). Contract R-UI-AUD-1. RED: `R-UI-AUD-1: payload with <script> renders literal text` + `R-UI-AUD-1: 403 guard for operator (no request)`. (M)
- [x] **T-5-3** `api/users.ts` + `pages/UsersPage.tsx`: list w/ role/active badges + pagination (R-UI-USR-1); create/invite form (username 3-50 regex, password 12-128 + strength hint, NO locale field asserted — R-UI-USR-2); 409 `USERNAME_TAKEN` localized; invitation toast without password echo. RED: `R-UI-USR-2: weak password client block` + `R-UI-USR-2: no locale input renders`. (M)
- [x] **T-5-4** User edit: role change + activate/deactivate (confirm-gated); self-deactivate blocked with localized warning (no request). Contract R-UI-USR-3. RED: `R-UI-USR-3: self-deactivate → warning, zero requests`. Deps: T-5-3, T-2-9. (S)
- [x] **T-5-5** `api/jobs.ts` + `pages/JobsPage.tsx`: list w/ queue/state/attempts, server-side state+queue filters (requires T-1-7 — replaces client-side fallback), failed rows distinct, retry confirm-gated → toast "Trabajo reencolado" + row refresh; 404 localized. Contracts R-UI-JOB-1/2. RED: `R-UI-JOB-1: state filter issues query param` + `R-UI-JOB-2: unknown id → localized 404`. Deps: T-1-7. (M)
- [x] **T-5-6** Governance page tests + evidence: notifications/audit/users/jobs page tests; `docs/output-ui-it5.txt`. (M)

## Iteration 6 — Polish + Evidence (it6) — ✅ COMPLETE (Batch E FINAL: 217 vitest, 256 backend, Docker SPA verified)

**Goal**: RBAC five-role sweep + mid-session demotion, full NFR scan suite, conditional backend verifications, Docker multi-stage + CI gate, README/dashboard evidence, cumulative green. **Exit**: all suites green (node --test + vitest), tsc clean both workspaces, `vite build` ok, CI workflow green, `docs/output-ui-it6.txt` — ✅ met (217 vitest, 256 backend, SPA served by the appliance).

- [x] **T-6-1** RBAC final pass: five-role sweep test (mount shell per role, exact sidebar+action set vs matrix — R-RBAC-2/3) + mid-session demotion / stale-403 toast (R-RBAC-4). RED: `R-RBAC-3: five-role sweep exact visible set` + `R-RBAC-4: demoted → sidebar drops create/edit on next me`. Files: `ui/tests/rbac.test.tsx` (extend). Deps: T-3..T-5 pages. (M)
- [x] **T-6-2** NFR scan suite: no-`any` scan (R-UI-NFR-1), no `dangerouslySetInnerHTML` with API data (R-UI-NFR-2), dist secret scan (R-UI-NFR-3), compiler-on + no useMemo/useCallback scan (R-UI-NFR-4), chunk-split check (R-UI-NFR-5), requirement-ID coverage scan — every R-XXX in specs appears in ≥1 test name (R-UI-NFR-6); WAREHOUSE_IN_USE present in localizeError parity (design flag b verify). RED: `R-UI-NFR-6: coverage scan green`. Files: `ui/tests/nfr.test.ts`. (M)
- [x] **T-6-3** ⚠️ **CONDITIONAL (design flag a)**: verify `GET /api/stock` excludes inactive products (join check at apply); if it does NOT, add small additive filter + backend test (R-UI-CRM-5 scenario). RED: `R-UI-CRM-5: inactive product absent from stock levels`. Files: `backend/src/modules/stock/*` (+ test). (S) — RESOLVED POSITIVELY at it1 (#1433, `WHERE p.active = true`); UI assertion ADDED at it6 in `ui/tests/stock.test.tsx`.
- [x] **T-6-4** Docker multi-stage: build stage adds `npm run typecheck -w ui` → `npm test -w ui` → `npm run build -w ui`; runtime `COPY --from=build /app/ui/dist ./ui/dist`; CI inserts ui typecheck/test/build before Docker build (R-PROD-3 modified); single container serves SPA at `/` (R-PROD-7). RED: `R-PROD-3: failing vitest fails workflow` (CI scan) + `R-UI-NFR-7: build -w ui → dist` + container serves SPA. Files: `capstone/Dockerfile`, `.github/workflows/ci.yml`, root scripts. (M)
- [x] **T-6-5** Polish + docs: README (stack, demo ES/EN, runbook), evidence dashboard-pages lists UI iterations, final empty/error-state + toast pass. Contracts R-PROD-6/7, R-UI-NFR-7. RED: README contains exact demo commands; dashboard lists it1..it6. Files: `README.md`, `docs/evidence/*`. (M) — dead `users.usernameTaken` catalog key removed (verified unused, parity green); flake mitigation `fileParallelism: false` (serial).
- [x] **T-6-6** Final evidence: full cumulative rerun (backend `node --test` + vitest + tsc both workspaces + vite build) + `docs/output-ui-it6.txt` (requirement-referenced green tests + ES/EN demo commands). (S)

---

## Estimation

| Iteration | Tasks | Difficulty mix | Approx lines |
|---|---|---|---|
| it1 backend delta | 9 | 2S/7M | ~500 |
| it2 foundation | 10 | 1S/6M/3L | ~2,600 |
| it3 dashboard+CRM | 8 | 1S/7M | ~1,600 |
| it4 stock+orders | 9 | 2S/5M/2L | ~2,000 |
| it5 governance | 6 | 1S/5M | ~1,200 |
| it6 polish+evidence | 6 | 2S/4M | ~800 |
| **Total** | **48** | 9S/34M/5L | **~8,700** |

Difficulty: S = small (≤1 file, ~50-150 lines), M = medium (1-3 files), L = large (cross-cutting or spike, >300 lines). Apply batches: it1 as batch A (backend delta must merge first); it2 batch B; it3+it4 batch C (it4 needs it3's modules); it5 batch D; it6 batch E.

## Review Workload Guard

Total estimated changed lines: **~8,500–9,000** (frontend ~7.5k, backend delta ~500, infra/docs ~800). **Recommendation: 6 chained PRs, one per iteration** (`ui-it1`..`ui-it6` → `main`), each merge gate = backend suite + vitest + tsc both workspaces + vite build + evidence doc. Largest PR = it2 (~2,600 lines) — optionally split into `ui-it2a` (bootstrap+i18n+client) + `ui-it2b` (auth+shell+components). it4 (atomic showpiece) and it1 (backend contract) are review-critical — schedule dedicated review.

## Priority / Sequencing (summary)

1. **it1 first** (S1) — UI modules consume real contracts; T-1-5/T-1-6/T-1-7 gate T-3-6/T-3-7/T-5-5.
2. **Bootstrap cluster T-2-1..T-2-3** gates all frontend work; spike (T-2-2) must conclude before any RTL test.
3. **i18n catalog pinned at T-2-4 (S3 GATE)** — parity (R-I18N-1) + banned-token (R-I18N-2) tests are CUMULATIVE: every later iteration's new strings must keep es≡en key parity and neutral Spanish or CI fails.
4. **Order within it2**: i18n → client → auth/guards → permissions → shell → components (S4); pages (it3+) build on all.
5. **it6 last**: conditional tasks resolve additively only (stock exclusion filter; WAREHOUSE_IN_USE verification); no new features.
6. **User checkpoints**: none inside iterations (all decisions locked; spike and conditionals are apply-phase, no user input required).