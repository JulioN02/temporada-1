{
  "name": "business-operations-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BOP v1 — Business Operations Platform (capstone, Temporada 1). API + worker + Postgres 16 appliance.",
  "workspaces": [
    "backend",
    "ui"
  ],
  "scripts": {
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w ui",
    "test": "npm test -w backend && npm test -w ui",
    "build": "npm run build -w ui"
  },
  "engines": {
    "node": ">=22.12"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
